package com.nanologic.eventify; // Replace with your actual package name

import com.bumptech.glide.annotation.GlideModule;
import com.bumptech.glide.module.AppGlideModule;

@GlideModule
public final class MyAppGlideModule extends AppGlideModule {
    // You can override methods here to customize Glide's behavior.
    // This example doesn't override anything, so it uses the default Glide configuration.
}
